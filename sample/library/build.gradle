plugins {
    id "com.android.library"
    id "kotlin-android"
    id "jvmCompat"
    alias(additionals.plugins.managed.device)
}

android {
    namespace 'eu.codlab.library'
}

dependencies {
    implementation additionals.androidx.core
    implementation additionals.eventbus
    implementation additionals.voxeet.promise
}

apply from: gradle.extended.files.publishing